import Image from "next/image";
import Head from "next/head";
import Layout from "../../components/layout";

export const meta = {
  siteTitle: "Call Apply And Blind",
};

<Head>
  <title>{meta.siteTitle}</title>
</Head>

# Call, Apply and Bind

export default ({ children }) => <Layout>{children}</Layout>;
